<h1>Restaurants:</h1>

<div class="container">
<ol>
<% @restaurants.each do |rest| %>
  <li class="card"><%= link_to rest.name, restaurant_path(rest), class: "btn btn-primary" %></li>
<% end %>
</ol>
</div>
